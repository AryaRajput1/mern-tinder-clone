import React from 'react'
import './css/icon.css'

function Options({icon,name,shadow}) {
    return (
        <div className='icon' >
            {icon}
        </div>
    )
}

export default Options
